import Colors from './Colors';
import Scale, { screenHeight, screenWidth, StatusBarHeight } from './Matrics';
import Images from './ImagePath';
import AppStyle from './AppStyle';

const Matrics = {
    screenHeight,
    screenWidth,
    StatusBarHeight
}

export { Colors, Scale, Matrics, Images, AppStyle };